spring:
  application:
    name: ms-apply-payment


  datasource:
    url: jdbc:mysql://localhost:3306/bd_prueba
    username: root
    password: usbw
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8083

logging:
  level:
    com.payment.ms-apply-payment: DEBUG